{
  "name": "sahni-backend",
  "version": "1.0.0",
  "description": "Backend API for Sahni Tata - Lambda Deployment Package",
  "main": "lambda.js",
  "type": "module",
  "scripts": {
    "package": "npm install --production && zip -r ../lambda-deployment.zip . -x '*.git*' -x '*.md' -x '*.log' -x 'scripts/*' -x 'test/*' -x '.env*'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1",
    "dotenv": "^16.3.1",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "swagger-ui-express": "^5.0.0",
    "swagger-jsdoc": "^6.2.8",
    "@aws-sdk/client-s3": "^3.490.0",
    "@aws-sdk/s3-request-presigner": "^3.490.0",
    "pg": "^8.11.3",
    "serverless-http": "^3.2.0"
  }
}

